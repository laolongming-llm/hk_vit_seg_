# ============================================================
# HK ViT Seg - Python dependencies
# Recommended Python version: 3.10
# ============================================================

# ----------------------------
# Deep learning core (training / eval / export)
# ----------------------------
torch>=2.5,<2.6
torchvision>=0.20,<0.21
torchaudio>=2.5,<2.6

timm>=1.0.0
einops>=0.7.0
albumentations>=1.4.0
opencv-python-headless>=4.9.0

numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
PyYAML>=6.0.0
rasterio>=1.3.0
matplotlib>=3.7.0
tensorboard>=2.14.0
tqdm>=4.66.0
rich>=13.0.0

# Hugging Face pretrained weight download/cache
huggingface_hub>=0.24.0
safetensors>=0.4.0

# ----------------------------
# GIS / data preparation scripts
# ----------------------------
# scripts/data_prep/* and scripts/dl/formal/07_* require GDAL (osgeo).
# On Windows, installing GDAL via conda-forge is usually more stable than pip.
gdal>=3.6.0
