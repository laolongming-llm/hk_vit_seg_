# seed3407 覆盖配置
# 说明：继承 base，仅覆盖 run_name / seed / run_subdir。
base_config: "configs/baseline_v1/train_vit_seg_baseline_v1_base.yaml"

experiment:
  run_name: "baseline_v1_seed3407"
  seed: 3407

output:
  run_subdir: "baseline_v1_seed3407"
