# seed42 覆盖配置
# 说明：继承 deit3_v1 base，仅覆盖 run_name / seed / run_subdir。
base_config: "configs/deit3_v1/train_deit3_v1_base.yaml"

experiment:
  run_name: "deit3_v1_seed42"
  seed: 42

output:
  run_subdir: "deit3_v1_seed42"
